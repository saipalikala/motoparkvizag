.env
.env.*
.env.local
.env.production
# ...but the templates carry no secrets and MUST be tracked: they are the only
# record of the correct variable names (a drifted name silently falls back).
!.env.example

# node modules
node_modules/
backend/node_modules/
motopark-web/node_modules/

# env files
.env
backend/.env
motopark-web/.env

# build files
dist/
build/
backend/uploads
uploads

# local IDE / agent tooling — not part of the app
.claude/
.codex/
.mcp.json
.playwright-mcp/
AGENTS.md